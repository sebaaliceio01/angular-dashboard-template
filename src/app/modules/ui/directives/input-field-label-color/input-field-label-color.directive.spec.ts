import { InputFieldLabelColorDirective } from './input-field-label-color.directive';

describe('InputFieldLabelColorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFieldLabelColorDirective();
    expect(directive).toBeTruthy();
  });
});
